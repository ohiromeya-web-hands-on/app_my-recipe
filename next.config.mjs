/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
  // TODO: add images.remotePatterns for Vercel Blob when recipe image rendering is implemented.
};

export default nextConfig;
